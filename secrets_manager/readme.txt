1> copy "secrets_manager/hooks/pre-commit"  to git hook folder at ".git/hooks"
2> All the project team members' public key is stored in "secrets_manager/public_keys" folder.
   Each key is stored in a file.  Their email address is the filename.
